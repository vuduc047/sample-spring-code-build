version: 0.2

phases:
  install:
    runtime-versions:
      java: amazoncorretto:17-alpine
      docker: 18
  build:
    commands:
      - echo Build started on `date`
      - mvn clean install
  post_build:
    commands:
      - echo Build completed on `date`
      - docker build -t sample .
